Variables mot, lettres en Chaîne
Début
  Ecrire "Entrez un mot : " \
  Lire mot
  Si mot = "un mot" Alors
    Ecrire "C'était ce qui était demandé ! Bravo !"
  SinonSi mot Alors
    Si Longueur(mot) > 1 Alors
      lettres <- "caractères"
    Sinon
      lettres <- "caractère"
    FinSi
    Ecrire "Le mot", mot, "contient", Longueur(mot), lettres
  Sinon
    Ecrire "Vous ne voulez pas jouer le jeu..."
  FinSi
Fin
