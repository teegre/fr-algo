# Test opérations arithmétiques.
Variables a, b, c en Entier
Début
  a ← 2
  b ← 3
  c ← a + b
  Ecrire "c est égal à a + b soit 2 + 3 =", c
  Ecrire "ajoutons 5 à c"
  c ← c + 5
  Ecrire "c est maintenant égal à", c
  Ecrire "c * 2 =", c * 2 
  Ecrire "a * b + 2 =", a * b + 2
  Ecrire "(a + 2) * b =", (a + 2) * b
  Ecrire "c au carré :", c ^ 2
  Ecrire "-c :", -c
  Ecrire "(2 * a) + ((b * 5) + 2) =", (2 * a) + ((b * 5) + 2)
Fin
